(ns jiksnu.xmpp.user-repository-test
  (:use clojure.test
        midje.sweet
        jiksnu.xmpp.user-repository))

(deftest test-get-data
  (testing "when the user id is a valid user"
    (testing "and the key is 'password'"
      (fact "should return the password"

        )
      )
    )
  )
