input {
  file {
    type => "syslog"

    # Wildcards work, here :)
    path => [ "/var/log/*.log", "/var/log/messages", "/var/log/syslog",
         "/home/duck/projects/jiksnu/logs/*.log"
     ]
  }
}

output {
  stdout { }
  elasticsearch { embedded => true }
}
