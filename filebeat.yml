output:
  logstash:
    enabled: true
    hosts: ["elk:5044"]
    timeout: 15
    insecure: true
    tls:
      certificate_authorities: ["/etc/pki/tls/certs/logstash-beats.crt"]

filebeat:
  prospectors:
    -
      paths:
        - "/app/logs/timbre-spit.log"
      document_type: json_lines
