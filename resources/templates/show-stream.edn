[:div
 [:div {:ng-if "!loaded"} [:spinner]]
 [:div {:ng-if "loaded"}
  [:p "{{stream.name}}"]
  #_[:p "{{stream.owner}}"]
  [:show-user-minimal {:data-id "{{stream.owner}}"}]
  [:debug {:expr "stream"}]
  [:subpage {:name "activities" :item "stream"}
   [:debug {:expr "$parent.page"}]

   ]]
 ]
