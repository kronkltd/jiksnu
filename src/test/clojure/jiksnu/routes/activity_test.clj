(ns jiksnu.routes.activity-test
  (:use clojure.test))

(deftest index-route-test
  (testing "when the user is not authenticated"
    (testing "and there are no activities"

      )
    (testing "and there are activities"

      )))
